import React from 'react';
import { Container, Typography} from '@mui/material';


const SocialLinks = () => {
  return (
    <Container sx={{ marginTop: '30px' }}>
      <Typography variant="h2" sx={{ marginBottom: '20px', textAlign: 'center' }}>Projects</Typography>
      <Typography variant="h4" sx={{textAlign: 'center'}} >PAGE STILL IN PROGRESS</Typography>
    </Container>
  );
};

export default SocialLinks;
